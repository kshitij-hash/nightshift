{
  "name": "nightshift",
  "version": "0.1.0",
  "private": true,
  "description": "Recurring private authorization on the STRK20 privacy pool: subscribe once inside the pool, get charged on schedule without ever surfacing.",
  "license": "Apache-2.0",
  "repository": "github:kshitij-hash/nightshift",
  "scripts": {
    "check:manifest": "node scripts/check-manifest.mjs",
    "build:console": "./site/node_modules/.bin/esbuild web/app.mjs --bundle --format=esm --outfile=web/app.bundle.mjs"
  },
  "dependencies": {
    "@starknet-io/get-starknet-core": "6.0.1",
    "starknet": "10.7.0"
  }
}
